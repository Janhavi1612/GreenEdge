import React from 'react';
import { CheckOutlined, CloseOutlined } from '@ant-design/icons';
import { Switch } from 'antd';

const Toggle = () => {
  return (
    <div>
      <Switch checkedChildren="" unCheckedChildren="" /> show only overlapping area
    </div>
  )
}

export default Toggle